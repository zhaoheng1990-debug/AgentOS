# Theory-First R4 Relation Boundary

Standalone zero-Provider validation for the R4 v0.3 semantic-relation and
deterministic-composition object chain.

It does not import or write AgentOS CoreSlim, Runtime, retention, or baseline
state.

Run:

```text
python -m pytest -q
python -m theory_first_r4_relation.cli
```
