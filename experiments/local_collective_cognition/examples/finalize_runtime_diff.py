"""Close and package v0.35 Runtime-derived candidate diff."""
from __future__ import annotations
import hashlib,json,sys,zipfile
from pathlib import Path
PACK_ROOT=Path(__file__).resolve().parents[1];REPO_ROOT=Path(__file__).resolve().parents[3]
sys.path[:0]=[str(REPO_ROOT/"agentos_core_slim_v0"),str(PACK_ROOT)]
from local_collective_cognition.provider_telemetry import hash_payload  # noqa: E402
def read(p): return json.loads(Path(p).read_text(encoding="utf-8"))
def write(p,v): Path(p).write_text(json.dumps(v,indent=2,sort_keys=True),encoding="utf-8")
def sha(p):
    h=hashlib.sha256()
    with Path(p).open("rb") as f:
        for b in iter(lambda:f.read(1048576),b""): h.update(b)
    return h.hexdigest()
def zpack(path,files):
    with zipfile.ZipFile(path,"w",zipfile.ZIP_DEFLATED) as z:
        for source in sorted(files,key=lambda x:x.name):
            i=zipfile.ZipInfo(source.name);i.date_time=(1980,1,1,0,0,0);i.compress_type=zipfile.ZIP_DEFLATED;i.external_attr=0o100644<<16;z.writestr(i,source.read_bytes())
def main():
    out=REPO_ROOT/"outputs"/"runtime_diff_v0_35";prior=read(out/"source_v0_34_analysis.json");post0=read(out/"source_v0_34_posthoc.json");close0=read(out/"source_v0_34_closure.json");corpus=read(out/"runtime_diff_corpus_frozen.json");prereg=read(out/"runtime_diff_preregistration.json");run=read(out/"runtime_diff_run.json");a=read(out/"runtime_diff_analysis.json")
    failed=sorted(k for k,v in a["conditions"].items() if not v);r=a["revision_metrics"];g=r["triggered_gross_candidate_uplift"];n=r["triggered_net_uplift"];m=a["pooled_contrast_metrics"]
    d_c={"diagnostic_version":"runtime_diff_diagnostics_v0_35","source_run_hash":run["run_hash"],"source_analysis_hash":a["artifact_hash"],"raw_receipt_count":len(run["raw_receipts"]),"trigger_receipt_count":len(run["trigger_receipts"]),"derived_diff_receipt_count":len(run["revision_receipts"]),"failures":run["failures"],"runtime_metrics":a["runtime_metrics"],"revision_metrics":r,"pooled_metrics":m,"failed_conditions":failed,"external_panel_present":False,"core_integration_authorized":False}
    d={**d_c,"artifact_hash":hash_payload(d_c)};write(out/"runtime_diff_diagnostics.json",d)
    recovery=read(out/"checkpoint_recovery.json")
    report="\n".join(["# Runtime-derived candidate diff v0.35","","## Observed facts",f"- Diff-minus-A1 mean/median: {m['mean']:.3f}/{m['median']:.3f}; wins {m['win_count']}/{m['count']}.",f"- Majority-positive case rate: {a['majority_positive_case_rate']:.3f}.",f"- Trigger rate: {r['trigger_rate']:.3f} ({r['trigger_count']} cells).",f"- Runtime-derived actions: {json.dumps(r['revision_action_distribution'],sort_keys=True)}; KEEP rate {r['keep_action_rate']:.3f}.",("- Gross revision mean/median: "+(f"{g['mean']:.3f}/{g['median']:.3f}." if g else "not observed.")),("- Net revision mean/median: "+(f"{n['mean']:.3f}/{n['median']:.3f}." if n else "not observed.")),f"- Mean worker token penalty: {r['mean_worker_token_penalty']:.3f} Cbit.",f"- Prompt/lineage coverage: {a['runtime_metrics']['prompt_identity_coverage']:.3f}/{a['runtime_metrics']['lineage_contract_coverage']:.3f}.",f"- Total tokens: {a['physical_total_tokens']}.",f"- Checkpoint recovery replayed Provider calls: {str(recovery['provider_calls_replayed']).lower()}.","- Failed frozen conditions: "+(", ".join(failed) if failed else "none")+".","","## Interpretation","- Provider returned content only; Runtime derived every action and inherited route.","- No action-label or route-echo failure can count as a cognitive result.","- The final run was reconstructed from the hash-validated last checkpoint after an analysis-only missing-trigger exception; Provider evidence was not replayed.","- All outcomes remain synthetic, external, candidate-only, and unable to write CoreSlim, retention, baseline, selection, or production state.","",f"Decision: `{a['decision']}`.",f"Candidate state: `{a['candidate_state']}`."])
    (out/"experiment_report.md").write_text(report,encoding="utf-8")
    l_c={"ledger_version":"runtime_diff_ledger_v0_35","source_prior_analysis_hash":prior["artifact_hash"],"source_prior_posthoc_hash":post0["artifact_hash"],"source_prior_closure_hash":close0["artifact_hash"],"source_corpus_hash":corpus["artifact_hash"],"source_preregistration_hash":prereg["artifact_hash"],"source_run_hash":run["run_hash"],"source_analysis_hash":a["artifact_hash"],"decision":a["decision"],"candidate_state":a["candidate_state"],"external_semantic_panel_authorized":a["external_semantic_panel_authorized"],"core_integration_authorized":False,"retention_authority":False,"production_authority":False};ledger={**l_c,"artifact_hash":hash_payload(l_c)};write(out/"ledger.json",ledger)
    rp_c={"replay_version":"runtime_diff_replay_v0_35","working_directory":str(PACK_ROOT),"commands":["python examples/prepare_runtime_diff.py","python examples/run_runtime_diff.py","python examples/finalize_runtime_diff.py"],"provider_model":run["model_id"],"frozen_corpus_hash":corpus["artifact_hash"],"warning":"Replay creates new Provider evidence."};replay={**rp_c,"artifact_hash":hash_payload(rp_c)};write(out/"replay.json",replay)
    rb_c={"rollback_version":"runtime_diff_rollback_v0_35","isolated_output_directory":str(out),"rollback_action":"Remove only this isolated output directory.","agentos_core_files_touched":False,"retention_or_baseline_mutation_performed":False};rollback={**rb_c,"artifact_hash":hash_payload(rb_c)};write(out/"rollback_pointer.json",rollback)
    c_c={"closure_version":"runtime_diff_closure_v0_35","source_ledger_hash":ledger["artifact_hash"],"candidate_state":a["candidate_state"],"decision":a["decision"],"runtime_diff_gate":a["runtime_diff_gate"],"external_semantic_panel_authorized":a["external_semantic_panel_authorized"],"core_integration_authorized":False,"promotion_allowed":False};closure={**c_c,"artifact_hash":hash_payload(c_c)};write(out/"closure.json",closure)
    names=["source_v0_34_analysis.json","source_v0_34_posthoc.json","source_v0_34_closure.json","runtime_diff_corpus_frozen.json","runtime_diff_preregistration.json","runtime_diff_progress.json","checkpoint_recovery.json","runtime_diff_run.json","runtime_diff_analysis.json","runtime_diff_diagnostics.json","experiment_report.md","ledger.json","replay.json","rollback_pointer.json","closure.json"]
    i_c={"inventory_version":"runtime_diff_inventory_v0_35","files":[{"path":name,"bytes":(out/name).stat().st_size,"sha256":sha(out/name)} for name in names]};inv={**i_c,"artifact_hash":hash_payload(i_c)};write(out/"hash_inventory.json",inv)
    pack=out/"runtime_diff_v0_35_return_pack.zip";zpack(pack,[out/name for name in names+["hash_inventory.json"]]);m_c={"manifest_version":"runtime_diff_manifest_v0_35","return_pack":pack.name,"return_pack_sha256":sha(pack),"return_pack_bytes":pack.stat().st_size,"inventory_hash":inv["artifact_hash"],"closure_hash":closure["artifact_hash"]};manifest={**m_c,"artifact_hash":hash_payload(m_c)};write(out/"manifest.json",manifest)
    print(json.dumps({"decision":a["decision"],"state":a["candidate_state"],"return_pack":str(pack),"sha256":manifest["return_pack_sha256"]},indent=2,sort_keys=True))
if __name__=="__main__": raise SystemExit(main())
