# R4 Role-Decomposed Semantic Inference Preregistration v0.3K

## Status

`PREREGISTERED_HUMANGATE_APPROVED_PROVIDER_CALLS_CONDITIONAL_ON_HASH_FREEZE`

## Frozen Sequence

1. commit the frozen theory, authorization, and preregistration;
2. construct sixteen fresh public cases and private references;
3. build only the modular standalone comparison instrument;
4. freeze corpus, prompts, schemas, compiler adapter, scorer, tests, and call
   order in a committed manifest;
5. pass deterministic leakage, equality, compiler, and fail-closed preflight;
6. enable exactly eight bounded Provider logical calls;
7. compose role fields and compile outcomes locally;
8. score and replay without repair or rerun;
9. preserve raw responses, negative results, and theory writeback.

## Provider Budget

- Provider: DeepSeek;
- requested model: `deepseek-v4-flash`;
- thinking: disabled;
- temperature: 0;
- logical calls: 8;
- maximum physical attempts: 16;
- maximum completion tokens per call: 8000;
- maximum total tokens: 100000.

## Fresh Corpus

- sixteen cases unused by prior Provider experiments;
- case IDs `R43K-01` through `R43K-16`;
- four provenance-edge cases;
- four effect-scope-edge cases;
- four cross-role interaction cases;
- four null or unknown-applicability cases;
- identical public evidence in every arm and round;
- exact private attribute labels and attribute-level evidence references;
- no v0.3J labeled case record, case text, or case identifier;
- ontology enum values remain unchanged and necessarily recur;
- no relation, Runtime action, revalidation label, private tuple, or extraction
  position in public prompts.

## Frozen Roles

Provenance-state:

- `SourceIdentity`;
- `LineageCoupling`;
- `TransformStatus`.

Effect-scope:

- `InformationEffect`;
- `AddedUncertainty`;
- `TargetClaimRelation`.

Runtime alone validates, joins, checks status-effect coherence, and compiles the
final relation, action, and revalidation trajectory.

## Matched Arms

Wide paired control:

- two independent full six-attribute calls per round;
- both wide prompts and schemas are byte-identical within the same case order;
- Runtime consumes provenance fields from the preassigned P-position call;
- Runtime consumes effect-scope fields from the preassigned E-position call;
- P and E extraction positions are never disclosed to Provider.

Role-decomposed:

- one provenance-state call per round;
- one effect-scope call per round;
- each call returns only its three responsibility-bounded attributes.

Both arms use the same evidence, two contexts per round, selected field source,
factor compiler, scorer, and private reference.

## Frozen Call Order

```text
wide_source_p_forward
split_provenance_forward
split_effect_forward
wide_source_e_forward
wide_source_e_reverse
split_effect_reverse
split_provenance_reverse
wide_source_p_reverse
```

Forward calls use ascending case order. Reverse calls use descending case
order.

## Primary Unit

One `case-role bundle` contains:

- the three role attributes;
- the exact evidence-reference set for those attributes.

There are 32 case-role bundles per arm and round.

## Frozen PASS Gates

All gates must pass:

1. deterministic corpus and implementation preflight passes;
2. public prompts contain no private state, extraction position, or Runtime
   authority;
3. eight logical calls complete within sixteen physical attempts;
4. total tokens do not exceed 100000;
5. sixteen receipts are present exactly once in every call;
6. every emitted evidence reference is admitted case-local evidence;
7. split exact role-bundle accuracy exceeds matched wide by at least 2/32 in
   both rounds;
8. every split semantic attribute is at least 14/16 in both rounds;
9. split witness exactness is not lower than matched wide for either role in
   either round;
10. split revalidation is 16/16 in both rounds;
11. split Runtime action accuracy is at least 15/16 in both rounds;
12. split Runtime action accuracy is not lower than matched wide in either
    round;
13. split forward-reverse action agreement is at least 15/16;
14. false combine and false deduplicate are zero in the split arm;
15. every incomplete or incoherent composed receipt fails closed;
16. no forbidden Provider field enters canonical state;
17. deterministic replay is byte-identical;
18. Provider decision authority and protected writes are zero.

Token and latency differences are reported as Pareto coordinates. Lower cost
cannot compensate for a failed cognition or harm gate.

## Failure Classification

- unequal evidence, call count, compiler, or leaked reference:
  `INCONCLUSIVE_IDENTIFIABILITY_FAILURE`;
- invalid corpus or scorer:
  `CONSTRUCTION_FAILURE`;
- local role gain with composition loss:
  `COMPOSITION_BOUNDARY_FAILURE`;
- split degradation:
  `GLOBAL_CONTEXT_REQUIREMENT`;
- no stable matched difference:
  `NO_DECOMPOSITION_BENEFIT`;
- order-dependent reversal:
  `PRESENTATION_DRIFT`;
- leading gates pass:
  `RESPONSIBILITY_BOUNDED_INTERFERENCE_SUPPORTED`.

## No Same-Version Repair

After the first Provider exposure, no corpus, reference, role grouping, prompt,
schema, extraction rule, threshold, compiler adapter, scorer, or call order may
change. Mechanical or semantic repair closes v0.3K and requires a new
preregistered version.

## Pre-Exposure Boundary Clarification

Before corpus hash freeze and with zero Provider calls, “no reused labels” was
clarified to mean no reused labeled case record. Requiring new ontology enum
values would change the tested object and make a matched comparison impossible.
No model, role boundary, threshold, call order, corpus result, or PASS gate
changed.
